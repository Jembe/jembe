[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.coverage.run]
source = ["{{project.name}}"]
branch = true


[build-system]
requires = ["setuptools","wheel"]
build-backend = "setuptools.build_meta"
